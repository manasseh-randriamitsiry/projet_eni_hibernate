PROJET 3 : GESTION DES SALLES DE CLASSE
    Utiliser Hibernate avec une base de données de type MySQL
I) LES CLASSES D’OBJET :
    PROF(codeprof, Nom, Prenom, Grade)
    SALLE(codesal, Designation)
    OCCUPER(codeprof, codesal, date)
    // bien choisir les type de données appropriés
II) TRAITEMENTS :
    -Côté client : Application web avec JSP
    -Mapper les 3 classes d’objet
    -CRUD sur les 3 tables
    -Recherche des profs par son code ou son nom